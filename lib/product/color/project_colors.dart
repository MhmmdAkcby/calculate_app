import 'package:flutter/material.dart';

class ProjectColor {
  static const greyColor = Colors.grey;

  static const blackColor = Color.fromARGB(255, 50, 48, 48);
  static const whiteColor = Colors.white;
  static const amberColor = Color.fromRGBO(255, 160, 0, 1);
}
